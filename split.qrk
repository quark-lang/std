{
  (import "list.qrk")
  (let split (fn (string delimiter) {
    (let result (list))
    (let tmp "")
    (foreach string (fn (char) {
      (if (= char delimiter)
        {
          (push result tmp)
          (set tmp "")
        }
        (set tmp (+ tmp char))
      )
    }))
    (if (> (length tmp) 0) (push result tmp))
    (return result)
  }))
}
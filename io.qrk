{
  (import "std.ts")
  (import "foreach.qrk")
  (let puts (fn ((list values)) {
    (foreach values (fn (item) {
      (stdout (+ item " "))
    }))
  }))
  (let print (fn ((list values)) {
    (print (values))
    (stdout (newline))
  }))
}
